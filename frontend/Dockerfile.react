# Dockerfile.react
FROM node:14 AS builder
WORKDIR /app
COPY package.json package-lock.json ./

# Node.js v14에 호환되는 npm 버전 설치
RUN npm install -g npm@6

# 패키지 설치
RUN npm install
COPY . .
RUN npm run build

FROM nginx:1.17.10
COPY --from=builder /app/build /usr/share/nginx/html/react
